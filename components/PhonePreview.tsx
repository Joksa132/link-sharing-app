import { ProfilePreview } from "@/lib/types";

export default function PhonePreview({ links, profile }: ProfilePreview) {
  return (
    <div className="bg-phone-img bg-[length:600px_505px] bg-center h-full w-full"></div>
  );
}
